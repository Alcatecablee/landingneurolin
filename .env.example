# NeuroLint Landing Page Environment Variables
# This is a frontend-only landing page that connects to the NeuroLint API backend

# API Configuration (Optional)
# The backend API URL - defaults to https://app.neurolint.dev/api if not set
VITE_API_URL="https://app.neurolint.dev/api"

# Analytics (Optional)
# Set to true to enable Vercel Analytics in production
VITE_ENABLE_ANALYTICS="false"
